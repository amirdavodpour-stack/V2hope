# HOPE V2 4.0.8+7 — Clean Test / Final Build Package

This archive contains only the source, tests, CI workflows, build tooling, and the
minimum operational documentation required to continue the remaining certification work.
Historical session reports, evidence archives, generated build output, dependency caches,
and agent handoff material have been removed.

## Local order

1. Qualify the runner: RAM >= 6144 MiB, Node 24.x, JDK 17 (`javac`), Flutter 3.47.2,
   Android SDK 36 / build-tools 36.0.0 / NDK 28.2.13676358, adb, Docker, PostgreSQL 16.
2. Run `bash tools/runtime-certification-preflight.sh` and do not bypass a failed guard.
3. Run `flutter pub get --enforce-lockfile`.
4. Run `flutter analyze` and `flutter test --no-pub`.
5. Run the Android debug/release build helpers under `tools/` as appropriate.
6. Run PostgreSQL, staging, provider, S3 and DR gates with real infrastructure/credentials.
7. Use `tools/release_candidate_check.sh` only after the external runtime gates are green.

The source tree itself does not claim final certification; final status must come from fresh
runtime evidence generated by the qualified runner.
